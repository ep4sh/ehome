apiVersion: v1
kind: Service
metadata:
  name: {{ include "ehome.fullname" . }}
  labels:
    {{- include "ehome.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ehome.selectorLabels" . | nindent 4 }}
